export default function Footer({ sidebarVisible, onToggleSidebar }) {
  return (
    <footer>
      {/* <button onClick={() => onToggleSidebar(!sidebarVisible)}>
        {sidebarVisible ? "Hide" : "Show"} Sidebar
      </button> */}
    </footer>
  );
}
